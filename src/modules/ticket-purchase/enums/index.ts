export enum PurchaseStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  RESOLD = 'resold',
}

export enum ResaleStatus {
  LISTED = 'listed',
  SOLD = 'sold',
  CANCELLED = 'cancelled',
}
